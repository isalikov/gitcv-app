import styled from 'styled-components'

export const Container = styled.div`
    position: fixed;
    right: 15px;
    bottom: 15px;
    height: 40px;
    display: flex;
    align-items: center;
    gap: 12px;
`

export const Title = styled.span`
    font-weight: 500;
    font-size: 12px;
`
